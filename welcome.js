

(() => {
  const { version, cwd } = process;

  console.log("Welcome to Node.js Assessment!");
  console.log(`Current Node.js version: ${version}`);
  console.log(`Current working directory: ${cwd()}`);
})();

